let transportationMode:string[]= [ "Bicycle","Airplane", "Train", "Car"]
for(let i=0; i<transportationMode.length; i++){

    console.log("I would like to own a " , transportationMode[i]);
    
}